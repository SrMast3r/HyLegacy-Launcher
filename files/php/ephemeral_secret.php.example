<?php
// Copia este archivo a "ephemeral_secret.php" (mismo directorio) en cada
// servidor (dev y producción) y reemplaza el valor por uno aleatorio propio,
// por ejemplo con: php -r "echo bin2hex(random_bytes(32));"
//
// Debe coincidir EXACTAMENTE con EPHEMERAL_CLIENT_KEY que build.js sustituye
// en el cliente al compilar. ephemeral_secret.php nunca se sube al repo
// (ver .gitignore) — igual que GH_UPDATE_TOKEN nunca queda en texto plano ahí.
return array(
    'client_key' => 'CAMBIA-ESTO-POR-UN-VALOR-ALEATORIO',
);
